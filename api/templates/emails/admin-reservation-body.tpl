<h1>Sikeres pályafoglalás!</h1>

<p>{$user->full_name} sikeresen lefoglalta a(z) {$reservation->timeunit->court->name} pályát {$reservation->timeunit->date} napon {$reservation->timeunit->from} - {$reservation->timeunit->to} időszakra.</p>

<p>Üdvözlettel: Kispest SE</p>